# Variables para base de datos
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=supervitec_db
DB_HOST=localhost
DB_PORT=5432

# Variables para autenticación (usadas en Etapa 1)
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRATION=3600  # 1 hora en segundos

# Variables para storage (descomenta si usas MinIO)
# MINIO_USER=minioadmin
# MINIO_PASSWORD=minioadminpassword
# MINIO_ENDPOINT=localhost:9000
# MINIO_BUCKET=supervitec-bucket